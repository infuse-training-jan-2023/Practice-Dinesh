AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS:Serverless-2016-10-31

Resources:

  MyBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: infuse-bucket-o1

  MyLambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: lambda-access-role
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
        - Effect: Allow
          Principal:
            Service: lambda.amazonaws.com
          Action: sts:AssumeRole
      Path: /
      Policies:
      - PolicyName: s3-access
        PolicyDocument:
          Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
              - s3:PutObject
            Resource: '*'

  Log_to_s3_bucket:
    Type: AWS::Serverless::Function
    Properties:
      Handler: lamda-function.lambda_handler
      Runtime: python3.9
      CodeUri: lamda-function.py
      Role: !GetAtt MyLambdaExecutionRole.Arn

  ApiGateway:
    Type: AWS::ApiGateway::RestApi
    Properties:
      Name: my-api-gateway

  ApiGatewayResource:
    Type: AWS::ApiGateway::Resource
    Properties:
      RestApiId: !Ref ApiGateway
      PathPart: resource
      ParentId: !GetAtt ApiGateway.RootResourceId

  ApiGatewayMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref ApiGateway
      ResourceId: !Ref ApiGatewayResource
      HttpMethod: POST
      AuthorizationType: NONE
      Integration:
        Type: AWS_PROXY
        IntegrationHttpMethod: POST
        Uri: !Sub arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${Log_to_s3_bucket.Arn}/invocations
      MethodResponses:
      - StatusCode: '200'

  MyDeployment:
    Type: AWS::ApiGateway::Deployment
    DependsOn: ApiGatewayMethod
    Properties:
      RestApiId: !Ref ApiGateway

  MyLambdaPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref Log_to_s3_bucket
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com

Outputs:
  EndpointURL:
    Description: URL of the endpoint
    Value: !Sub "https://${ApiGateway}.execute-api.${AWS::Region}.amazonaws.com/MyDeployment/resource"
  BucketName:
    Description: Name of the S3 bucket
    Value: !Ref MyBucket